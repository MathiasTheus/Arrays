const numbers = [1,2,3,4,5];
let sum = 0; // variável para armazenar a soma
for(let i = 0; i < numbers.length; i++){ // percorre o array
    sum = sum + numbers[i]; // soma cada elemento ao total
}   
